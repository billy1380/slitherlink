enum Edge {
  empty,
  line,
  nLine,
}

enum Number {
  none,
  zero,
  one,
  two,
  three,
}

enum Orientation {
  up,
  down,
  left,
  right,
  upFlip,
  downFlip,
  leftFlip,
  rightFlip,
}

enum LoopCell {
  unknown,
  exp,
  noExp,
  out,
}

enum Difficulty {
  easy,
  hard,
}
